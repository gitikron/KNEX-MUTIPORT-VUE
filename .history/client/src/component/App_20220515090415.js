function App() {
    const AppHeader = < div className = 'App' >
        const textInput = < input type = "text" / > ;
    return ({ AppHeader }
        h2 > HEADER < /h2> 

        <
        p > { textInput } < /p>

        <
        /
        div >
    );
}

export default App;