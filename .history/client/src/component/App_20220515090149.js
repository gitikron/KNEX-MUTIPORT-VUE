const textInput = < input type = "text" / >

    function App() {

        return ( <
            div className = 'App' >

            <
            h2 > HEADER < /h2> < /
            div >
            <
            p > { textInput } < /p>
        );
    }

export default App;