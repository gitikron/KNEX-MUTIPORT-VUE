function App() {
    const textInput = < input type = "text" / >
        return ( <
            div className = 'App' >

            <
            h2 > HEADER < /h2> < /
            div >
            <
            p > { textInput } < /p>
        );
}

export default App;