function App() {
    const textInput = < input / >
        return ( <
            div className = 'App' >

            <
            h2 > HEADER < /h2> < /
            div >
        );
}

export default App;